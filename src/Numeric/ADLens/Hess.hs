{-# LANGUAGE RankNTypes, FlexibleContexts #-}
module Numeric.ADLens.Hess
    ( 
    ) where


--type HessAD a b c = Vector t -> (Vector t, Vector t -> Vector t, GMatrix t, Vector t -> GMatrix t)
-- we can't use sparse vectors?
